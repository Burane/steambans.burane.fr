version: "3.3"

services:
  app:
    container_name: steambans
    restart: always
    build: .
    ports: 
      - "3008:3008"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27018:27017"

 